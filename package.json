{
  "name": "platform-config",
  "version": "1.0.0",
  "description": "Shared platform configuration, feature flags, and JSON schemas",
  "scripts": {
    "validate": "node scripts/validate.js",
    "test": "node scripts/validate.js"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "js-yaml": "^4.1.0"
  }
}
